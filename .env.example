# ═══════════════════════════════════════════════════════════════
# ClydeCodeBot Configuration
# ═══════════════════════════════════════════════════════════════

# ─── REQUIRED ───────────────────────────────────────────────

# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# Comma-separated Telegram user IDs allowed to use the bot
# Get yours: message @userinfobot on Telegram
ALLOWED_USER_IDS=

# ─── AUTH (pick one) ────────────────────────────────────────
#
# Option 1 (recommended): Claude Code OAuth
#   Run `claude` in your terminal and login with your Pro/Max account.
#   The SDK will use that session automatically. No key needed.
#
# Option 2: API Key
# ANTHROPIC_API_KEY=sk-ant-xxxxx

# ─── WORKSPACE ──────────────────────────────────────────────

CLYDECODEBOT_WORKING_DIR=
CLYDECODEBOT_MODEL=
CLYDECODEBOT_PERMISSION_MODE=default
CLYDECODEBOT_ALLOWED_TOOLS=Read,Glob,Grep,WebSearch
CLYDECODEBOT_SYSTEM_PROMPT=
CLYDECODEBOT_MAX_TURNS=0
CLYDECODEBOT_USE_PROJECT_SETTINGS=true

# ─── OPENCLAW MEMORY (optional) ────────────────────────────

OPENCLAW_PATH=
CRASHCART_PATH=
CLYDECODEBOT_INCLUDE_DAILY_LOG=true

# ─── PERMISSION GATE ────────────────────────────────────────
#
# Separate Telegram bot for task approvals (setup wizard on first run)
CLYDECODEBOT_REQUIRE_PERMISSION=true
PERMISSION_BOT_TOKEN=

# Bash command prefixes that skip approval
# CLYDECODEBOT_AUTO_ALLOW_BASH=openclaw-memo,openclaw-custodian,clawcrashcart,docker

# ─── AUDIT CHAIN ────────────────────────────────────────────
#
# Independent AI models review each tool proposal before execution.
# Configure auditors in ~/.clydecodebot/auditors.json (see README)
#
CLYDECODEBOT_AUDIT_ENABLED=true
CLYDECODEBOT_AUDIT_CONSENSUS=single
# Consensus: single (first auditor), majority (>50%), unanimous (all)

# Risk thresholds for autonomous execution
CLYDECODEBOT_AUTO_APPROVE_RISK=2
# ≤2 + all auditors approve → silent auto-execute
CLYDECODEBOT_ALERT_RISK=3
# ≤3 + all approve → auto-execute with notification
# 4 + all approve → ask human
# 5 → always block

# Path to custom auditors config (default: ~/.clydecodebot/auditors.json)
# CLYDECODEBOT_AUDITORS=

# ─── AUDITOR API KEYS ──────────────────────────────────────
#
# Keys resolved in order: ClawVault → env var → manual prompt
# Custom providers: <PROVIDER_UPPER>_API_KEY (e.g., DEEPSEEK_API_KEY)
#
# OPENAI_API_KEY=
# GOOGLE_API_KEY=
# KIMI_API_KEY=
